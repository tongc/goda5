<?xml version="1.0"?>
<ruleset name="My company's EJB checker rules">
	<description>
		The Design Ruleset contains a collection of rules that find questionable
		designs.
	</description>
	<rule name="CustomRule"
		message="Don't create threads, use the MyCompanyThreadService instead"
		class="com.goda5.hagendaz.common.util.CustomRule">
		<description>
			Don't create Threads, use the MyCompanyThreadService instead.
		</description>

		<example>
<![CDATA[
 Thread t = new Thread(); // don't do this!
]]>
		</example>
	</rule>
</ruleset>